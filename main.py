"""
Ponto de entrada do projeto BeAContab — ISS Fortaleza.

ATENÇÃO: Este projeto opera exclusivamente via interface gráfica (GUI).
Para iniciar a aplicação, execute:

    python app.py

Este arquivo (main.py) é mantido por compatibilidade histórica e não possui
função ativa desde que o projeto migrou para o modelo de execução GUI-only.
A CLI foi descontinuada — não há módulo 'extrair_nf_pdfs' disponível.
"""

import sys


if __name__ == "__main__":
    print("=" * 60)
    print("BeAContab — ISS Fortaleza")
    print("=" * 60)
    print()
    print("Este projeto opera exclusivamente via interface gráfica.")
    print("Para iniciar a aplicação, execute:")
    print()
    print("    python app.py")
    print()
    sys.exit(0)
